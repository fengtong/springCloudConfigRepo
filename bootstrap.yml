spring:
  profiles:
    active: development
  cloud:
    config:
      server:
        git:
          uri: https://github.com/fengtong/springCloudConfigRepo.git
  application:
    name: mcroservice-config-server
